# https://editorconfig.org
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 2

# Recorded verbatim from real ss, lsof, /proc and PowerShell output. An editor
# that trimmed a trailing space or added a final newline would change what the
# parsers are tested against, which is the one thing these files must not do.
[test/fixtures/**]
trim_trailing_whitespace = false
insert_final_newline = false
end_of_line = unset

[*.py]
indent_size = 4

[*.md]
trim_trailing_whitespace = false
